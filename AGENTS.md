# Repository Guidelines

- This package is build-time TypeScript shared by PHP Skir code generators.
- Keep the core independent of DTO libraries and framework-specific output models.
- Preserve compatibility for generator adapters when evolving public exports.
- Add or update focused Vitest coverage before changing behavior.
